{
  "name": "chat-api",
  "version": "1.0.0",
  "description": "Real time chat app",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "dev": " nodemon server",
    "test": " mocha --recursive || true",
    "testReal": "echo 'Continue if testing db is selected else kill <ctrl> + z' && read message && mocha --recursive  || true"
  },
  "author": "Akash no",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "moment": "^2.29.4",
    "mongoose": "^6.5.0",
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "nodemon": "^2.0.19"
  }
}
